{
    "name": "FTDSS",
    "dockerFile": "Dockerfile",
    "mounts": [
        "source=${localEnv:USERPROFILE}/.gitconfig,target=/root/.gitconfig,type=bind,consistency=cached",
        "source=${localWorkspaceFolder}/.fortls,target=/root/.fortls,type=bind,consistency=cached"
    ],
    "runArgs": [
        "--hostname=ftdss-dev",
        "--privileged",
        "--cap-add=SYS_ADMIN",
        "--cap-add=SYS_PTRACE",
        "-v",
        "/sys/kernel/debug:/sys/kernel/debug"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "MS-CEINTL.vscode-language-pack-ja",
                "ms-vscode.cpptools",
                "ms-azuretools.vscode-docker",
                "fortran-lang.linter-gfortran",
                "PKief.material-icon-theme",
                "ms-python.python",
                "tamasfe.even-better-toml",
                "ritwickdey.LiveServer",
                "ms-vscode.cmake-tools"
            ]
        }
    },
    "postCreateCommand": "sudo apt-get update"
}